// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id            String    @id
  name          String
  email         String
  emailVerified Boolean
  image         String?
  createdAt     DateTime
  updatedAt     DateTime
  sessions      Session[]
  accounts      Account[]

  @@unique([email])
  @@map("user")
}

model Session {
  id        String   @id
  expiresAt DateTime
  token     String
  createdAt DateTime
  updatedAt DateTime
  ipAddress String?
  userAgent String?
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([token])
  @@map("session")
}

model Account {
  id                    String    @id
  accountId             String
  providerId            String
  userId                String
  user                  User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  accessToken           String?
  refreshToken          String?
  idToken               String?
  accessTokenExpiresAt  DateTime?
  refreshTokenExpiresAt DateTime?
  scope                 String?
  password              String?
  createdAt             DateTime
  updatedAt             DateTime

  @@map("account")
}

model Verification {
  id         String    @id
  identifier String
  value      String
  expiresAt  DateTime
  createdAt  DateTime?
  updatedAt  DateTime?

  @@map("verification")
}

model HeroSection {
  id               String   @id @default("hero")
  bgImageUrl       String
  titlePart1       String
  description      String
  tag              String?
  titlePart2       String?
  primaryBtnText   String?
  primaryBtnLink   String?
  secondaryBtnText String?
  secondaryBtnLink String?
  updatedAt        DateTime @updatedAt

  @@map("hero_section")
}

model VisiMisiSection {
  id           String   @id @default("visimisi")
  visionTitle  String   @default("VISI")
  visionText   String
  missionTitle String   @default("MISI")
  missionText  String
  updatedAt    DateTime @updatedAt

  @@map("visi_misi_section")
}

model CoreValueSection {
  id          String          @id @default("corevalues")
  badge       String          @default("Our Foundation")
  title       String          @default("Core Values")
  description String          @default("The driving principles that guide our engineering precision, telemetry accuracy, and commitment to service quality.")
  items       CoreValueItem[]
  updatedAt   DateTime        @updatedAt

  @@map("core_value_section")
}

model CoreValueItem {
  id          String           @id @default(cuid())
  icon        String           @default("Lightbulb")
  title       String
  description String
  order       Int              @default(0)
  sectionId   String           @default("corevalues")
  section     CoreValueSection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  @@map("core_value_item")
}

model HistorySection {
  id          String             @id @default("history")
  badge       String             @default("Our Journey")
  title       String             @default("History & Milestones")
  description String             @default("Over five decades of powering industrial operations, scaling our services, and pioneering telemetry solutions.")
  items       HistoryMilestone[]
  updatedAt   DateTime           @updatedAt

  @@map("history_section")
}

model HistoryMilestone {
  id          String         @id @default(cuid())
  icon        String         @default("Milestone")
  year        String
  description String
  order       Int            @default(0)
  sectionId   String         @default("history")
  section     HistorySection @relation(fields: [sectionId], references: [id], onDelete: Cascade)

  @@map("history_milestone")
}

// 🎯 New model to store contact form submissions
model ContactMessage {
  id        String   @id @default(cuid())
  name      String
  company   String?  // optional
  email     String
  phone     String?  // optional
  message   String
  createdAt DateTime @default(now())

  @@map("contact_message")
}



